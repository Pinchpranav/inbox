# build artifacts & deps (rebuilt inside the image)
node_modules
dist
dist-ssr

# local secrets (keep .env.production + .env.example — they're committed)
.env
.env.local
.env.*.local

# vcs / editor / os
.git
.gitignore
.vscode
.idea
.DS_Store
Thumbs.db

# logs
*.log
npm-debug.log*
pnpm-debug.log*